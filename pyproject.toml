[project]
name = "riganything-mini"
version = "0.1.0"
description = "Deep learning pipeline for 3D character rigging prediction (RigNet-style)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "open3d>=0.19.0",
]
